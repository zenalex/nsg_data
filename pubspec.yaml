name: nsg_data
description: Data object model and data exchange with C# server
version: 0.1.0
homepage: https://github.com/zenalex/nsg_data
publish_to: 'none'

environment:
  sdk: '>=2.15.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  shared_preferences: ^2.0.6
  flutter_multi_formatter: ^2.2.0
  dio: ^4.0.0
  get: ^4.6.5
  retry: ^3.1.0
  event: ^2.0.5
  pedantic: ^1.11.1
  intl: ^0.17.0
  jiffy: ^5.0.0 
  uuid: ^3.0.6

dev_dependencies:
  

flutter:
  uses-material-design: true

  assets:
    - lib/assets/logo-wfrs.png
    - lib/assets/no_image.jpg